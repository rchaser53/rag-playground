# OpenAI
# - APIキーを設定すると「埋め込み検索」と「LLM回答」が有効になります
OPENAI_API_KEY=

# 任意（未設定なら既定値を使用）
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4o-mini

# Server
PORT=8787

# Data
# - SQLite DB の保存先ディレクトリ
DATA_DIR=./data
